# Database Configuration
POSTGRESDB_USER=postgres
POSTGRESDB_ROOT_PASSWORD=postgres123
POSTGRESDB_DATABASE=rec_system
POSTGRESDB_LOCAL_PORT=5431          # Внешний порт (localhost:5431)
POSTGRESDB_DOCKER_PORT=5432         # Внутренний порт в Docker контейнере

# Application Configuration
# Для локальной разработки и production сервера (одинаковые порты):
NODE_LOCAL_PORT=6868                # Внешний порт (localhost:6868 или geos.icc.ru:6868)
NODE_DOCKER_PORT=8080               # Внутренний порт в Docker контейнере
PORT=8080                           # Порт приложения внутри контейнера
DEBUG=false
ENABLE_CRON=true

# Database Connection (used by app when running locally without Docker)
DB_HOST=localhost
DB_PORT=5431                        # Используйте POSTGRESDB_LOCAL_PORT при локальном запуске
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=rec_system

# SSL Configuration (optional)
SSL_ENABLED=false
SSL_KEY_PATH=/certs/privkey.pem
SSL_CERT_PATH=/certs/fullchain.pem

# External API
CRIS_BASE_URL=http://cris.icc.ru
API_TIMEOUT=90

# File Paths
CSV_FILE_PATH=calls.csv
RECOMMENDATIONS_FILE_PATH=recomendations.json
KNN_SCRIPT_PATH=knn.py

